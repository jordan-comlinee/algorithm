import sys
'''
def isComplicated(time_prev, time_cur, diff, level) :
    return (time_cur + time_prev) * (diff - level) + time_cur

def calculateTotalTime(diffs, times, limit, minLevel):


def solution(diffs, times, limit):
    answer = 0
    high = max(int)
    print(high)
    return answer
'''