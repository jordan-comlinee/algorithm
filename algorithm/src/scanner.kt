import java.util.*

fun main(args: Array<String>) {
    // 여러 변수를 입력받아야 하는 경우 (Int)
    //var inputInt = Scanner(System.`in`)
    //println("${inputInt.nextInt()+inputInt.nextInt()}")

    var input = Scanner(System.`in`)
    println("${input.next()}")

}