#include <stdio.h>
#include <bits/stdc++.h>
using namespace std;
int dx[] = { -1, 1, 0, 0 };
int dy[] = { 0, 0, -1, 1 };
int cnt = 0;
void dfs() {

}

int main(void) {
	int n, m;
	scanf_s("%d %d", &n, &m);
	vector<vector<int>> maze(n, vector<int>(n));
	vector<int> m_list(m);

	printf("%d", cnt);
	return 0;
}