#include <iostream>
#include <sstream>
#include <vector>
using namespace std;

// 총 점수를 구해야 하므로, 총 점수를 global하게 둔다.
int totalScore = 0;

vector<int> round = { 5, 6, 7, 8, {17, 18}, {9, 10}, {11, 12}, {19, 20}, 25 };

// 라운드의 조건에 맞도록 게임을 진행함(최대 9라운드)
int solution(vector<int> param) {
	int initScore = 40;


	return 0;
}