#include <bits/stdc++.h>
using namespace std;

int N;
vector<pair<int, int>> papers;

void input_data() {
	cin >> N;
	papers.resize(N);
	for (int i = 0; i < N; ++i) {

	}
}

int main() {
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	input_data();
}