#include <string>
#include <vector>

using namespace std;

int solution(vector<vector<int>> scores) {
    int answer = 0;
    vector<int> wonho = scores[0];
    scores.erase(scores.begin() + 0);
    return answer;
}